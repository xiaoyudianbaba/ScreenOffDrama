# 息屏听剧（ScreenOffDrama）项目分析报告

## 项目概述
**应用名称**：息屏听剧（ScreenOffDrama）  
**应用类型**：Android 原生应用  
**开发语言**：Kotlin  
**目标平台**：Android 16（兼容 Android 12+）  
**当前版本**：v1.1  
**项目状态**：已完成两阶段开发

## 核心功能
1. **息屏听剧**：在 YouTube/哔哩哔哩播放视频时，点击悬浮球使屏幕全黑，声音继续播放
2. **电源键退出**：按电源键可退出息屏状态，返回原视频界面
3. **YouTube 广告自动跳过**：通过无障碍服务自动检测并跳过 YouTube 广告
4. **连续广告支持**：支持多段广告的自动逐段跳过

## 技术架构
### 构建配置
- **Gradle 版本**：8.11.1
- **Kotlin 版本**：2.1.20
- **Android SDK**：compileSdk 36, targetSdk 36, minSdk 31
- **Java 版本**：JDK 17

### 依赖库
- `androidx.core:core-ktx:1.17.0`
- `androidx.activity:activity-ktx:1.10.1`

### 项目结构
```
ScreenOffDrama/
├── app/
│   ├── src/main/java/com/screenoffdrama/
│   │   ├── MainActivity.kt          # 主界面
│   │   ├── Whitelist.kt             # 白名单管理
│   │   ├── service/
│   │   │   ├── ScreenOffService.kt  # 息屏前台服务
│   │   │   └── AdSkipService.kt     # 广告跳过无障碍服务
│   │   └── overlay/
│   │       ├── FloatingBallManager.kt  # 悬浮球管理
│   │       └── BlackOverlayManager.kt  # 黑色覆盖层管理
│   └── build.gradle.kts
├── build.gradle.kts
├── settings.gradle.kts
└── 息屏听剧-v1.0-debug.apk, 息屏听剧-v1.1-debug.apk
```

## 关键技术实现
### 1. 前台服务
- 服务类型：`mediaPlayback`
- 权限声明：`FOREGROUND_SERVICE_MEDIA_PLAYBACK`
- 使用 `ServiceCompat.startForeground()` 启动

### 2. 悬浮窗技术
- **悬浮球**：`TYPE_APPLICATION_OVERLAY` + `FLAG_NOT_FOCUSABLE`
- **黑色覆盖层**：全屏覆盖，包括状态栏、导航栏和挖孔区域
- 使用 `LAYOUT_IN_DISPLAY_CUTOUT_MODE_ALWAYS` 实现完整覆盖

### 3. 无障碍服务（广告跳过）
- 仅监听 `com.google.android.youtube` 包名
- 多策略检测：文本匹配、资源 ID 匹配、父节点遍历
- 防重复机制：点击后 1 秒内不重复点击

### 4. 电源键监听
- 监听 `ACTION_SCREEN_OFF` 广播
- 息屏模式下按电源键退出息屏状态

## 权限需求
1. **悬浮窗权限**：显示悬浮球和黑色覆盖层
2. **前台服务权限**：保持服务在后台运行
3. **无障碍服务权限**：YouTube 广告自动跳过
4. **忽略电池优化**：防止系统杀死服务
5. **通知权限**（Android 13+）：显示常驻通知

## 构建与使用
### 构建方式
1. 命令行：`gradlew.bat :app:assembleDebug`
2. Android Studio：打开项目根目录，Sync 后 Run

### 使用步骤
1. 安装 APK 并打开应用
2. 按引导开启所需权限
3. 打开 YouTube 或哔哩哔哩播放视频
4. 点击悬浮球进入息屏模式
5. 按电源键退出息屏状态

## 已知问题与限制
1. **屏幕超时**：屏幕自然超时会触发 `ACTION_SCREEN_OFF`，退出息屏
2. **后台播放**：物理熄屏后视频是否继续播放取决于应用自身策略
3. **国产 ROM 限制**：可能需要额外设置自启动和后台运行权限
4. **Play 商店政策**：无障碍服务和电池优化忽略功能受政策限制

## 版本历史
- **v1.1**：新增 YouTube 广告自动跳过功能
- **v1.0**：第一阶段最小可用版，实现息屏听剧全流程

## 项目文件清单
- `息屏听剧-v1.0-debug.apk` - v1.0 调试版本
- `息屏听剧-v1.1-debug.apk` - v1.1 调试版本
- `README.md` - 项目说明文档
- `screen_*.png` - 应用界面截图（共 12 张）
- 完整的 Android 项目源代码

## 总结
息屏听剧是一个功能完整的 Android 应用，实现了在播放视频时息屏继续听音的核心需求，并集成了 YouTube 广告自动跳过功能。项目结构清晰，技术实现合理，考虑了 Android 16 的新特性要求。应用已提供调试版本 APK，可直接安装测试使用。